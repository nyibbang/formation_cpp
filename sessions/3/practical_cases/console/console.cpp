#include "types.hpp"
#include <iostream>

std::ostream& operator<<(std::ostream& os, const Board& board) {
    for (auto slot : board.value) {

    }
    return os;
}

int main(int argc, char** argv) {
    return 0;
}
