#include <algorithm>
#include <iterator>
#include <vector>
#include <string>
#include <sstream>
#include <iostream>

// TODO: Ecrire une fonction qui va lire une liste de chaines de caracteres depuis 'input'
//       (séparées par un espace), va rechercher dans cette liste la chaine 'token'.
//       Puis va effectuer une rotation sur la liste afin de transformer
//       [s1, s2, ..., token, s3, s4, ...] en [token, s3, s4, ..., s1, s2, ...] et
//       enfin va copier la concaténation des strings dans 'output' séparées par des points
//       virgules ';'. Le résultat final doit terminer par un point virgule.
//
//       Interdiction d'utiliser une boucle !

void use_algorithms(std::string token, std::istream& input, std::ostream& output)
{
  // TODO
}
